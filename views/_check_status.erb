<% if @check == :checkmate %>
  <div class="alert-blurb">
    <%= "#{@player.color.capitalize} Player, your King is in CHECKMATE!" %>
  </div>
<% elsif @check == :check %>
  <div class="caution-blurb">
    <%= "#{@player.color.capitalize} Player, your King is in CHECK!" %>
  </div>
<% end %>
